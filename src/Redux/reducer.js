// Define the initial state
const initialState = {
  pageSize: null, // not in  use
};

// Define the reducer
export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
