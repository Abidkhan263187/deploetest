import { Box, FormControl, Grid, InputLabel, MenuItem, Select, Typography } from "@mui/material";

const Dashboard = () => {
    return (
        <Box component="main" container spacing={2} alignItems="center">

            <Typography variant="h6" component="h2" >
                Dashboard (MTD)
            </Typography>

        </Box>
    );
};

export default Dashboard;
